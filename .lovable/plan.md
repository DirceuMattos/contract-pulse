Ajustar os tokens de cor do modo claro em `src/index.css` para suavizar ainda mais o fundo e os cards em destaque:

1. Alterar `--background` de `240 12% 97.5%` para um tom levemente mais acinzentado, por exemplo `240 10% 95.5%`, reduzindo o brilho e mantendo a harmonia com a paleta navy.
2. Alterar `--card` e `--popover` de `0 0% 100%` (branco puro) para um tom quase branco suave, por exemplo `240 10% 98.5%`, para que os quadros em destaque fiquem mais suaves sem perder contraste com o fundo.

Nenhuma outra propriedade, arquivo ou rotina será alterada.